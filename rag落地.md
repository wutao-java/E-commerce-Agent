下面是我们讨论后的**一期 RAG 落地方案**。目标是回答商品、活动、售后等稳定知识问题；订单、库存、物流、退款进度等实时信息仍由业务接口提供，不放进知识库当作实时事实。

```text
文档 → 结构化解析 → 语义父子分块 → 向量化 → 版本化发布到 Milvus
用户问题 → 场景/有效期过滤 → 稠密+稀疏召回 → 融合、重排
         → 读取命中子块及父块 → 有依据则生成带引用回答，否则澄清或兜底
```

**一、文档处理**

支持 Markdown、Word 和 PDF，统一解析为标题层级、段落、列表、表格及来源位置，再切片。Markdown 按语法结构处理；Word 保留标题样式和表格，不能凭段落推算页码。PDF 要逐页区分可提取文本、扫描件和图文混排页：分别做文本提取、OCR、版面分析与阅读顺序恢复。双栏、表格、否定条件是重点验收项；解析质量不足的高风险规则不应自动发布。

**二、父子分块**

父块是一条能独立读懂的规则，尽量包含**适用条件、结论、例外**；子块按语义段落拆分，用于精确召回。初始可试父块约 600–1200 tokens、子块约 150–350 tokens，再依据真实规则和评测调整。子块边界保留少量相邻语义或标题上下文，但不跨章节、活动版本和生效规则重叠。

按我们最终讨论的选择，**每条 Milvus 子块记录同时保存 `child_text` 和 `parent_text`**：只对用于检索的子块生成向量，命中后直接读取父块补全语境。接受父块的少量重复存储，换取简单的在线链路。

**三、模型与混合检索**

优先验证百炼的 `qwen3.7-text-embedding-flash`，默认从 1024 维开始。[百炼文档](https://help.aliyun.com/zh/model-studio/embedding)说明它支持同次输出 `dense&sparse`；北京地域文档标价为 0.000125 元/千输入 Token，双路输出不额外增加该次生成费用。稀疏输出需要 DashScope SDK/API，实施前必须用实际账号确认返回结构，不能直接复用只取稠密向量的 OpenAI 兼容调用。

BGE-M3 保留为对照方案：它能生成稠密和 *learned sparse* 向量，但**learned sparse 不等于 BM25**；若要求通过百炼使用，还须确认具体托管接口确实暴露双路结果。第一期先做“稠密 + 模型稀疏”两路，不预先叠加第三路 BM25。文档向量和查询向量使用同一模型配置，并分别按 `document`、`query` 类型生成。

**四、存储字段与版本**

| 存储位置 | 职责 |
| --- | --- |
| 原文件存储 | 保存上传的原始文件及必要的解析产物。 |
| MySQL | 管理稳定的 `doc_id`、不可变的 `doc_version_id`、原文件哈希、审核发布状态、生效时间、解析和模型配置。 |
| Milvus 子块集合 | 存 `chunk_id`、`kb_id`、`doc_id`、`doc_version_id`、`parent_id`、子块序号、`child_text`、`parent_text`、稠密/稀疏向量，以及主题、适用范围、生效时间、文件名、标题路径和可用的页码范围。 |

`doc_id` 表示同一份知识，`doc_version_id` 表示某次不可变内容，`release_id` 表示对外服务的索引发布批次。**上传新版本不等于立即替换旧规则**：先解析入库、完成质量检查，再发布；普通问题只检索当前有效版本，明确问历史时才进入历史检索。初期知识库较小时，可构建完整的新发布集合并切换 Milvus 别名，便于一致性切换和回滚；不依赖逐条更新 `is_latest` 来模拟原子发布。更换 embedding 模型也必须重建索引，不能把两种模型的向量混在同一检索空间。

**五、在线回答与验收**

当前可把知识检索接在 [Agent 意图编排](D:/E-commerce-agent/agent/customer_service_agent.py)之后。先过滤知识主题、权限及规则有效期；两路各粗召回约 20–40 个子块，融合去重后给 reranker 约 20–30 个候选，最终按上下文预算选约 3–5 个父块作依据。这些 `top_k` 都是**测试起点**，不是上线常量。回答应引用命中子块的文件、章节和页码；证据不足时不输出确定性规则结论。实时问题转向关联 Spring Boot 项目的业务接口。

实施前先准备覆盖新旧活动、售后例外、精确商品词、复杂 PDF 和知识库外问题的标注集，分别验收**解析正确率、粗召回是否漏掉正确规则、重排后的证据命中、回答引用一致性、错误硬答率及延迟**。目前尚待实测确认的关键点是百炼 flash 的双路 API 返回、你们 PDF 样本的解析质量，以及上述分块和候选数在真实问题集上的效果；本轮没有修改工程代码。